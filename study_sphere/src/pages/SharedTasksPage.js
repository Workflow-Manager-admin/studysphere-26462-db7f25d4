import React from "react";

// PUBLIC_INTERFACE
function SharedTasksPage() {
  /** The page for viewing, adding, and managing shared tasks (mocked). */
  return (
    <div>
      <h2>Shared Tasks</h2>
      {/* Task list management comes later */}
      <p>This is where shared tasks will be managed.</p>
    </div>
  );
}

export default SharedTasksPage;
