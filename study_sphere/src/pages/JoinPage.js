import React from "react";

// PUBLIC_INTERFACE
function JoinPage() {
  /** The user join page where user can enter their name and start a session. */
  return (
    <div>
      <h2>Join StudySphere</h2>
      {/* Form implementation comes later */}
      <p>Enter your name to begin.</p>
    </div>
  );
}

export default JoinPage;
